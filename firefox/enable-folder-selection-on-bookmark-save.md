# Enable asking Bookmark folder on saving Bookmarks

Enter `about:config` 

Set `browser.bookmarks.editDialog.showForNewBookmarks` as `true`

Save a new bookmark and you will be asked for which folder
